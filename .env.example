# ===========================================
# FraudShield AI - Environment Configuration
# ===========================================
# Copy this file to .env and fill in your values
# cp .env.example .env

# -------------------------------------------
# Server Configuration
# -------------------------------------------
API_HOST=0.0.0.0
API_PORT=8000
DEBUG=true

# -------------------------------------------
# Provider Selection
# -------------------------------------------
# LLM Provider for generating explanations
# Options: azure_openai | openai | ollama | mock
# Default: mock (no API keys required)
LLM_PROVIDER=mock

# Pattern Matching Provider for fraud pattern detection
# Options: azure_search | local_json | mock
# Default: local_json (uses app/data/fraud_patterns.json)
PATTERN_PROVIDER=local_json

# -------------------------------------------
# Azure OpenAI Configuration
# (Required when LLM_PROVIDER=azure_openai)
# -------------------------------------------
AZURE_OPENAI_ENDPOINT=https://your-resource.openai.azure.com/
AZURE_OPENAI_API_KEY=your-api-key-here
AZURE_OPENAI_DEPLOYMENT=gpt-4o
AZURE_OPENAI_API_VERSION=2024-02-15-preview

# -------------------------------------------
# OpenAI API Configuration
# (Required when LLM_PROVIDER=openai)
# -------------------------------------------
OPENAI_API_KEY=sk-your-api-key-here
OPENAI_MODEL=gpt-4o

# -------------------------------------------
# Ollama Configuration (Local LLM)
# (Required when LLM_PROVIDER=ollama)
# -------------------------------------------
OLLAMA_BASE_URL=http://localhost:11434
OLLAMA_MODEL=llama3.2

# -------------------------------------------
# Azure AI Search Configuration
# (Required when PATTERN_PROVIDER=azure_search)
# -------------------------------------------
AZURE_SEARCH_ENDPOINT=https://your-search.search.windows.net
AZURE_SEARCH_API_KEY=your-search-api-key-here
AZURE_SEARCH_INDEX=fraud-patterns

# -------------------------------------------
# Frontend Configuration
# -------------------------------------------
NEXT_PUBLIC_API_URL=http://localhost:8000
